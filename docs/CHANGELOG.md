## Запуск backend (Windows)
```powershell
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload

## Запуск Telegram-бота (Windows)
```powershell
cd telegram-bot
.\.venv\Scripts\Activate.ps1
python main.py


###новый чат
Ты новый чат
🐾 Контекст проекта: Хвостосовет (2025–2026)

Это Telegram-бот / сервис помощи владельцам домашних животных.

Архитектура (MVP):
Telegram-бот → Backend API → Supabase (Postgres) → LLM → Backend → Telegram-бот

Ключевые принципы:

- Telegram-бот — тонкий клиент (UX + state)
- Backend — вся бизнес-логика, память, тарифы, LLM
- Free / Pro разделены backend-first
- Фото — часть вопроса, без отдельного UX
- .env и .venv не коммитятся

📄 Основные файлы (обязательно читать)

1️⃣ docs/STATUS.md
Источник истины по текущему состоянию проекта:

- что уже сделано,
- что считается стабильным,
- какие решения зафиксированы.

2️⃣ docs/FILEMAP.md
Карта проекта:

- где какой код живёт,
- какие есть главные потоки,
- «Где менять X» (быстрые ссылки).

3️⃣ docs/API.md (важно, см. ниже)
Контракт backend API:

- какие эндпоинты существуют,
- какие поля приходят/уходят,
- что гарантировано backend’ом клиенту.

Я работаю через VS Code + Codex.
Ты присылаешь чёткие инструкции / ТЗ, я копирую их в Codex, он правит код, после чего я локально тестирую (smoke) и, если всё ок, делаю commit.
Если что-то не так — возвращаюсь с логами или JSON-ответом для точечного фикса.








## 2026-01-03
### Backend — TTL-сессии (Free)

- Реализованы TTL-сессии для Free-пользователей:
  временная память диалога хранится в таблице `sessions`.
- В `session_context` сохраняется хвост диалога в формате:
  `turns: [{q, a}, …]`.
- Контекст активен, пока `expires_at > now()`; при каждом новом сообщении TTL продлевается.
- Количество хранимых пар вопрос-ответ ограничено `SESSION_MAX_TURNS`.
- Перед вызовом LLM backend формирует контекстный префикс из предыдущих turn’ов.
- Новый turn (`q/a`) сохраняется **только после успешного ответа LLM**.

Параметры:
- `SESSION_TTL_MIN` — время жизни активной сессии (в минутах).
- `SESSION_MAX_TURNS` — максимальное количество хранимых пар `q/a`.

Проверка:
- Отправить вопрос A и уточнение B (с новым `X-Request-Id`).
- Убедиться, что ответ на B учитывает контекст A.
- Проверить наличие `q/a` в `sessions.session_context` через SQL (`jsonb_path_exists`).


## 2026-01-03
### База данных / Backend

- Добавлено ограничение `CHECK` для `users.plan` — допускаются только значения `'free'` и `'pro'`.
- Обновлён внешний ключ `sessions.user_id → users.id` с `ON DELETE CASCADE`
  (при удалении пользователя его сессии удаляются автоматически).
- Зафиксировано поведение Supabase UI:
  превью `jsonb` может обрезаться и визуально не показывать все поля.
  Для корректной проверки `session_context` следует использовать SQL:
  `jsonb_path_exists(...)` и оператор `#>>`.

Проверка:
- Проверить ограничения через `pg_constraint` для таблиц `users` и `sessions`.
- Проверить наличие `q/a` в `sessions.session_context` через SQL, а не через Table Editor.

## 2026-01-03
### Telegram-бот

- Исправлено: второй вопрос пользователя не отправлялся в backend.
- Причина: профиль очищался сразу после ответа.
- Теперь профиль сохраняется, а бот переводится в состояние ожидания следующего вопроса.
- Добавлены управляемые debug-логи через переменную окружения BOT_DEBUG.
- Исправлен фильтр Pyrogram для текстовых сообщений (исключение команд).

Проверка:
- Задать вопрос → получить ответ → задать второй вопрос → backend получает оба запроса.
- BOT_DEBUG=1 — логи включены, BOT_DEBUG=0 — логи выключены.

## 2026-01-04
### Backend — session_context v1, режимы и единый контекст

- Добавлена поддержка режима (`mode`) в запросе `/v1/chat/ask` (опционально).
  Режим обновляется **только** если `mode` передан явно; иначе используется текущий `active.mode`.
- Введён формат `sessions.session_context` v1:
  - `active: { mode, updated_at }`
  - `turns: [{ t, mode, q, a }, ...]`
  - `summary: ""` (заготовка, пока не используется)
- Принято решение: **единый контекст диалога** для LLM.
  Контекстный префикс формируется из последних `SESSION_MAX_TURNS` пар `q/a` **по всем режимам** (без фильтрации по `mode`).
- Ограничение размера истории сделано общим: `turns` обрезается до последних `SESSION_MAX_TURNS`.
- Исправлена конфигурация TTL: `SESSION_TTL_MIN` установлен на 60 минут (ранее был 1 минута, что приводило к созданию новых сессий слишком часто).

Проверка:
- Задать 2 вопроса подряд → должна использоваться одна и та же сессия (`expires_at` продлевается).
- Сменить режим кнопкой (передать `mode`) и задать вопрос → `active.mode` обновляется, но контекст включает предыдущие turns (единый хвост).
- В Supabase проверить:
  - `jsonb_array_length(session_context->'turns') <= SESSION_MAX_TURNS`
  - `session_context #>> '{active,mode}'` содержит текущий режим.

## 2026-01-04
### Backend — промпты и режимы

- Промпты перенесены из telegram-bot в backend (`backend/app/services/prompts.py`).
- Введён единый словарь `PROMPTS_BY_MODE`: `emergency`, `care`, `vaccines`.
- Выбор `system_prompt` выполняется в backend по `active.mode`.
- Зафиксирован fallback на `"emergency"` (без зависимости от `DEFAULT_MODE`).
- `system_prompt` передаётся в OpenAI первым сообщением (`role=system`).
- Добавлены логи выбора режима: `CHAT_PROMPT active_mode=... selected_mode=...`.

Проверка:
- Отправить запросы с `mode=care` и `mode=vaccines`.
- Убедиться в различии ответов и наличии `CHAT_PROMPT` в логах.

## 2026-01-04
### Telegram-бот — унификация режимов (health → vaccines)

- Канонические режимы бота: `emergency`, `care`, `vaccines`.
- Режим `health` удалён как рабочий, оставлен только для legacy-совместимости.
- Добавлена единая нормализация режима:
  `strip/lower → health → vaccines → fallback emergency`.
- Все `context/current_mode` проходят через нормализацию перед отправкой в backend.
- Кнопка «💉 Прививки и профилактика» и callback_data переведены на `vaccines`.
- Локальный вызов OpenAI из бота удалён (`telegram-bot/services/llm.py`).

Проверка:
- Нажать «💉 Прививки и профилактика» → задать вопрос.
- В логах backend: `CHAT_PROMPT active_mode=vaccines selected_mode=vaccines`.
- Поиск `health` в telegram-bot допускается только в legacy-regex и маппинге.

## 2026-01-04
### Telegram-бот — тонкий клиент

- Вынесен HTTP-вызов backend в `telegram-bot/services/backend_client.py`.
- Из `handlers/question.py` удалён прямой `urllib` и сетевой код.
- Бот не содержит OpenAI / LLM-логики, работает только через backend API.

Проверка:
- Поиск `OpenAI|openai|gpt|chat.completions` → 0 совпадений.
- Поиск `urllib` → только `backend_client.py`.

---

## 2026-01-04
### Документация — API и smoke

- Добавлен контракт API: `docs/API.md`.
- Обновлены `README_telegram-bot.md` и `README_backend.md` (режимы, `session_context v1`).
- Обновлён `DEV_SMOKE`: добавлены проверки `mode` и сохранения активной сессии.

Проверка:
- `/v1/chat/ask` с `mode=care`, затем без `mode` → режим сохраняется.
- В логах backend: `CHAT_PROMPT active_mode=care`.

## 2026-01-04  
### Backend + Telegram-бот — лимиты Free/Pro и UX ошибок

#### Backend
- В ответ `POST /v1/chat/ask` добавлен объект `limits`:
  - `plan: free | pro`
  - `remaining_today`
  - `reset_at` (ISO8601, UTC)
- `remaining_today` считается из реального дневного лимита (`FREE_DAILY_LIMIT`).
- Поведение rate limit (HTTP 429) **не изменено**.
- Контракт зафиксирован в `docs/API.md`.

#### Telegram-бот
- В конец ответа добавлено отображение лимитов:
  - `🆓 План: Free · Осталось сегодня: N`
  - `💎 План: Pro`
- Бот **не вычисляет лимиты**, только отображает данные backend.
- Добавлен единый UX для ошибок backend:
  - `429` — лимит Free исчерпан (дружелюбное сообщение).
  - backend недоступен (`status=0`) — отдельное сообщение пользователю.
  - `401/403` — ошибка авторизации между ботом и backend.
  - `5xx` — временная недоступность сервиса.
- Happy-path ответа не изменён.

#### Инфраструктура разработки
- Удалён лишний корневой `.venv` (устранена путаница интерпретаторов).
- Зафиксировано правило: один `.venv` на сервис (`telegram-bot/.venv`, `backend/.venv`).

Проверка:
- Задать несколько вопросов → `remaining_today` уменьшается.
- Довести до лимита → бот показывает сообщение о превышении.
- Остановить backend → бот показывает сообщение о недоступности.
- Проверить “сырой JSON” `/v1/chat/ask` → `reset_at` строкой ISO8601.

## 2026-01-05  
### Backend + Telegram-бот — upsell при 429 и smoke бота

#### Backend
- При `HTTP 429` добавлен upsell для Free-пользователей:
  - `limits.upsell { type, title, text, cta }`.
- Гарантировано наличие `limits.reset_at` при любом 429
  (daily limit + cooldown).
- В ответ 429 добавлено поле `cooldown_sec` (из `COOLDOWN_SEC`).

#### Telegram-бот
- При 429 добавлена кнопка **«Оформить Pro»** (по данным backend).
- Реализован callback `upsell_pro` (заглушка).
- Строка `Сброс: reset_at` намеренно не отображается в UX.
- Happy-path и логика лимитов не изменены.

#### Документация / smoke
- Подготовлен блок smoke-проверки Telegram-бота
  (happy-path, 429 + upsell, backend down).
- Блок оформлен в `DEV_SMOKE_WITH_BOT.md` для копипаста в `DEV_SMOKE.md`.

Проверка:
- Довести Free до лимита → сообщение + кнопка upsell.
- Нажать кнопку → ответ-заглушка.
- Проверить JSON 429 → `limits.reset_at` присутствует.

## 2026-01-06  
### Telegram-бот — pending question, UX Free и команды

#### Telegram-бот
- Реализован flow **pending question** для Free-пользователей:
  - первый текст пользователя сохраняется как `pending_question`;
  - backend **не вызывается**, лимит **не тратится**;
  - пользователю предлагается дописать детали или продолжить через кнопки.
- Поддержаны два пути продолжения:
  - **Путь A (ручной):** детали одним сообщением → `pending_question + details` → 1 запрос в backend;
  - **Путь B (кнопки):** сценарий кнопок (вид → режим → базовая инфа) с сохранением `pending_question`.
- Добавлен шаг `pending_details`; `pending_question` сохраняется при `start_profile`.
- Вынесен helper для отправки запросов в backend (единая точка для обычного и pending-флоу).
- Free-UX явно подчёркивает отсутствие памяти о питомце (без блокировки и без списания лимита).

#### Команды / навигация
- Добавлена команда `/help` с краткой справкой и дисклеймером.
- Команда `/menu` реализована как алиас `/start`.
- Меню команд минимальное; отдельный `/reset` не добавлялся (роль выполняет `/start`).

#### Косметика
- Исправлены сломанные эмодзи в коде (комментарии и логи запуска).

Проверка:
- Написать вопрос без кнопок → получить сообщение «вопрос принят», лимит не уменьшается.
- Путь A: дописать детали → ответ приходит одним запросом.
- Путь B: пройти кнопки → ответ приходит без повторного ввода вопроса.
- `/help` и `/menu` работают в приватном чате.

## 2026-01-07  
### Telegram-бот — Pro-A профиль питомца (UX, анкета, подготовка к памяти)

#### Telegram-бот
- Реализован **Pro-A профиль питомца** (анкета) в Telegram:
  - шаги: вид → **кличка** → возраст → пол → порода → вес/кондиция (опционально);
  - вес принимается в форматах `6`, `6 кг`, `6,2`, `около 6`.
- Добавлены опциональные блоки профиля:
  - **Особенности здоровья** (категории + текст);
  - **Прививки и профилактика** (статусы, без дат);
  - **Важное о питомце** (free text).
- Анкета не блокирует основной сценарий:
  - добавлена кнопка **«Вернуться к вопросу»**;
  - поддержан `pending_question` → **1 вызов backend после завершения анкеты**.
- Реализована защита UX:
  - если шаг ожидает кнопку, а пользователь пишет текст — шаг не продвигается, повторяется клавиатура.
- Улучшены тексты post-меню:
  - различие **«Профиль создан»** / **«Профиль обновлён»**;
  - использование клички питомца в тексте (fallback — «питомца»).
- Профиль передаётся в payload `/v1/chat/ask` (backend пока не сохраняет).
- Добавлены явные debug-логи вызова backend (`[BACKEND] calling ...`).

#### Архитектура / подготовка
- Pro-A профиль хранится в state Telegram-бота (json), **без изменений backend и БД**.
- Заложена основа для следующего шага: сохранение профиля в backend/Supabase.

Проверка:
- Пройти анкету Pro-A → нажать **«Вернуться к вопросу»** → backend вызывается ровно 1 раз.
- Ввести вес в формате `6 кг` → значение принимается.
- После заполнения доп.блоков показывается текст **«Профиль обновлён»**.
- При вводе текста вместо кнопок бот просит нажать кнопку и не двигает шаг.

## 2026-01-08  
### Backend + Telegram-бот — память питомца (pets) и remembered-UX (эксперимент)

#### Backend
- Подтверждена запись анкеты Pro-A в таблицу `pets` (Supabase).
- Реализовано разрешение **эффективного профиля питомца**:
  - приоритет `payload.pet_profile` / `payload.pet`;
  - fallback — профиль из БД (`pets.profile`) для активного питомца.
- Профиль питомца добавляется в prompt перед вызовом LLM, из-за чего ответы начинают учитывать данные анкеты.
- Добавлены диагностические логи по профилю питомца (наличие, ключи).
- Усилена устойчивость записи профиля:
  - upsert питомца не ломает основной запрос;
  - запись пропускается при отсутствии `pet.type` (`pets.type NOT NULL`);
  - защита от `aborted transaction` при ошибках записи.

#### Telegram-бот
- Экспериментальная интеграция remembered-UX:
  - `/start` сначала пытается загрузить активного питомца через backend (`GET /v1/pets/active`);
  - при успехе показывается экран **«Помню вашего питомца»** с кнопками:
    - **❓ Задать вопрос**
    - **✏️ Обновить профиль**.
- Выполнена пробная маршрутизация Pro-переходов:
  - «Задать вопрос» → выбор сценария → планировался пропуск `basic_info` для Pro;
  - «Обновить профиль» → переход в Pro post-меню (вернуться / здоровье / прививки / важное).
- Выявлено, что крупные правки в `question.py` и `menu.py` ломают UX (тексты/эмодзи) и Free-flow.

#### Git / процесс
- После экспериментальных изменений выполнен откат незакоммиченных правок к последнему коммиту ветки (`git restore` / `reset --hard HEAD`).
- Зафиксировано решение продолжать интеграцию remembered-UX **малыми изолированными патчами**, без массового переписывания логики бота.

Проверка:
- `POST /v1/chat/ask` с `pet_profile` → ответы LLM учитывают анкету питомца.
- `GET /v1/pets/active?telegram_user_id=...` возвращает активного питомца.
- После отката: `git status` → `working tree clean` на ветке `backend-mvp-v0`.

## 2026-01-09  
### Telegram-бот — Pro remembered-UX, post-menu, явное сохранение профиля

#### Архитектура Telegram-бота
- Введён отдельный слой **flows/** для изоляции сложной логики:
  - создана папка `telegram-bot/flows/`;
  - Pro-логика вынесена в `flows/pro_flow.py`;
  - `question.py` оставлен тонким роутером (Free + делегирование в Pro-flow).
- Зафиксировано архитектурное правило:
  - **Free-flow и Pro-flow не смешиваются**;
  - сложные сценарии (анкета, post-меню, remembered-UX) живут в `flows/`.

#### Telegram-бот
- Переработан стартовый UX:
  - `/start` и «🏠 В меню» всегда показывают **единое главное меню** (без запросов в backend);
  - добавлены пункты **⭐ Мой питомец** и **ℹ️ Как это работает / Тарифы**;
  - загрузка активного питомца (`GET /v1/pets/active`) выполняется **только** по кнопке «Мой питомец».
- Стабилизирован **remembered-UX для Pro**:
  - при наличии профиля показывается экран с действиями **❓ Задать вопрос** / **✏️ Обновить профиль**;
  - при отсутствии профиля — предложение заполнить анкету.
- Исправлена маршрутизация callback’ов:
  - `pet_profile_update` больше не попадает в Free-меню режимов;
  - выбор вида питомца ограничен `pet_(dog|cat|other)`.
- Реализован корректный Pro-флоу:
  - **«Обновить профиль»** → post-меню (здоровье / прививки / важное);
  - **«Задать вопрос»** → выбор режима → пропуск `basic_info` → сразу ввод вопроса;
  - добавлен флаг `skip_basic_info` без обращений к БД.
- Починен ввод в post-меню:
  - текстовые шаги (здоровье и т.п.) больше не считаются вопросом и не вызывают LLM;
  - после ввода пользователь возвращается в post-меню.
- Нормализовано хранение профиля в state:
  - в `pet_profile` сохраняется **только профиль**, без `id` и вложенного `profile`;
  - устранён баг «json в json» при сохранении.
- Реализовано **явное сохранение изменений профиля (Variant A)**:
  - введён dirty-флаг `profile_dirty`;
  - кнопка **💾 Сохранить изменения** появляется только при несохранённых правках;
  - сохранение выполняется по нажатию кнопки через `/v1/chat/ask` с техническим текстом `__save_profile__`;
  - ответ LLM игнорируется, профиль сохраняется в `pets.profile`;
  - добавлен UX **«⌛️ Сохраняю…»** и защита от двойного клика (`profile_saving`).
- Исправлены критичные сценарии:
  - Free-пользователь больше не попадает в Pro-анкету;
  - Pro-обновления не триггерят генерацию ответа.

#### State / UX-инварианты
- Добавлены и стабилизированы флаги:
  - `skip_basic_info`, `profile_dirty`, `profile_saving`;
- Чётко разделены состояния:
  - **анкета / post-меню / вопрос** не пересекаются.

Проверка:
- Pro: изменить здоровье → увидеть кнопку **💾 Сохранить изменения** → сохранить → данные появляются в `pets.profile`.
- Pro: «Задать вопрос» → выбрать режим → сразу ввод вопроса → ответ учитывает сохранённый профиль.
- Free: полный сценарий работает без Pro-анкеты и без регрессий.

## 2026-01-10  
### Backend + Telegram-бот — PATCH-сохранение профиля и UX здоровья

#### Backend
- Исправлено сохранение профиля питомца:
  - реализована **PATCH-семантика** при `__save_profile__`;
  - частичные обновления **не затирают** базовые поля питомца и другие блоки профиля.
- Логика upsert питомца усилена:
  - при обновлении сохраняются существующие значения колонок (`name`, `sex`, `age_text`, `breed` и др.), если они не пришли в patch;
  - `pets.profile` (jsonb) обновляется через merge, а не replace;
  - вложенные объекты (`health`, `vaccines`, `parasites` и т.п.) дополняются корректно.
- Сохранение профиля окончательно выведено из LLM-пайплайна:
  - `__save_profile__` не вызывает LLM и не пишет в `sessions`;
  - операция стала быстрой и устойчивой к partial payload’ам.
- Нормализован профиль питомца:
  - поле `species` удалено из сохраняемых данных;
  - каноническим источником вида питомца остаётся `type`.

#### Telegram-бот
- Починен UX блока **«Особенности здоровья»**:
  - введена двухуровневая навигация:
    - подменю здоровья (кожа/шерсть, аллергия, ЖКТ, …, ✅ Готово);
    - post-меню профиля (сохранить / вернуться / др.).
  - после ввода подпункта пользователь **возвращается в подменю здоровья**, а не сразу в post-меню;
  - можно заполнить несколько подпунктов подряд и завершить блок кнопкой **«✅ Готово»**.
- Исправлена логика сохранения профиля:
  - данные из подпунктов здоровья дополняются, а не перезаписывают профиль;
  - сохранение выполняется одним действием по кнопке **💾 Сохранить изменения**.
- Устранено дублирование вида питомца в профиле (`type` + `species`).

Проверка:
- Создать полный профиль → сохранить.
- Обновить только один подпункт здоровья (например, аллергию).
- Проверить, что базовые данные и остальные блоки профиля **сохранились**.
- В подменю здоровья можно заполнить несколько пунктов подряд → **«✅ Готово»** → **💾 Сохранить изменения**.

## 2026-01-10  
### Telegram-бот — расширение анкеты питомца, стабилизация Free/Pro, откат skip-UX

#### Telegram-бот — анкета питомца
- Расширена базовая анкета Pro-профиля:
  - добавлен пункт **стерилизация / кастрация** (`profile.sterilized_status = yes | no | unknown`);
  - пункт участвует в PATCH-сохранении и не затирается при частичных обновлениях.
- Доработан блок **«Прививки и профилактика»**:
  - зафиксирован порядок шагов:
    1) `vaccines.status`
    2) `vaccines.details` (опциональный текст)
    3) `parasites.status`
    4) `parasites.details` (опциональный текст)
  - `details` для вакцин и паразитов хранятся **раздельно**;
  - при выборе **«Пропустить»** данные **не затираются** (`null` не записывается);
  - сохранение выполняется одним действием через **💾 Сохранить изменения**.
- Улучшена поддержка питомцев типа **«Другой»**:
  - вместо вопроса «Порода» используется вопрос **«Какое у вас животное?»**;
  - значение сохраняется в `profile.animal_kind`;
  - поле `breed` для `type=other` не используется.

#### Free / Pro — тестирование и конфигурация
- Зафиксировано архитектурное правило:
  - **`FORCE_PRO` в telegram-боте не используется** (вызывает рассинхрон с backend);
  - тестирование Free/Pro выполняется через поле `users.plan` в БД.
- `FORCE_PRO` признан dev-хаками и не применяется в текущем стабильном сценарии.

#### Откат / стабилизация
- Выполнен откат к последнему стабильному коммиту:
  - временно **отключена кнопка «⏭ Пропустить»** на шаге «Опишите питомца» при входе через главное меню;
  - логика анкеты Pro, PATCH-сохранения и remembered-UX сохранена и работает стабильно.
- Экспериментальная логика ленивой подгрузки профиля при входе через главное меню откатена
  (планируется отдельной задачей, без регрессий).

Проверка:
- Pro: заполнить анкету (включая стерилизацию, вакцины/паразиты) → **💾 Сохранить изменения** → данные корректно сохраняются.
- Pro: обновить только вакцины или паразитов → остальные блоки профиля сохраняются.
- `type=other`: в анкете сохраняется `animal_kind`, профиль не ломается.
- Free/Pro: план корректно определяется через БД, без `FORCE_PRO`.

## 2026-01-10  
### Backend — remembered-анкета Pro в `/chat/ask`, упрощение бота

#### Backend
- Реализован **автоматический учёт анкеты питомца для Pro** внутри `POST /v1/chat/ask`:
  - если в запросе `pet_profile` пустой или минимальный (`{type}`) — backend подтягивает активного питомца из БД;
  - логика применяется **только для Pro-пользователей**;
  - для Free анкета из БД **никогда не подгружается**.
- Зафиксированы приоритеты источников профиля:
  - **request > db (Pro only) > none**;
  - “богатый” профиль из запроса не перезатирается данными из БД.
- Добавлена нормализация `pet_profile`:
  - удаляются служебные поля (`step`, `context`, `current_mode`, `question`);
  - корректно определяется “минимальный профиль”.
- Улучшено логирование:
  - добавлен источник профиля `source=request|db|none`;
  - упрощена отладка remembered-UX.

#### Telegram-бот
- Бот снова выступает как **тонкий клиент**:
  - используется **один запрос** `POST /v1/chat/ask` для генерации ответа;
  - исключена ленивaя подгрузка анкеты (`GET /v1/pets/active`) в вопросном флоу.
- Кнопка **«⏭ Пропустить»** не выполняет сетевых запросов и только ускоряет переход к вопросу.
- Осознанно зафиксировано продуктовое поведение:
  - **несохранённые изменения анкеты (черновики) не влияют на ответы**;
  - для предотвращения потери данных планируется отдельный dirty-guard (P0).

Проверка:
- Pro: вход через **Собака / Кошка / Другой** → задать вопрос → анкета учитывается автоматически.
- Pro: вход через **⭐ Мой питомец** → задать вопрос → используется профиль из запроса или БД.
- Free: анкета из БД не подгружается ни при каком сценарии.
- В логах backend: `CHAT_PET_PROFILE source=db` (Pro) / `source=request` (Free).

## 2026-01-10  
### Backend + Telegram-бот — отвязка сохранения профиля от `/chat/ask`

#### Backend
- Сохранение анкеты питомца **полностью вынесено** из `POST /v1/chat/ask`.
- Добавлен отдельный endpoint **`POST /v1/pets/active/save`**:
  - сохраняет активный профиль питомца;
  - **не вызывает LLM** и **не пишет в sessions**;
  - работает с **PATCH-семантикой (merge)** — частичные данные не затирают профиль;
  - доступен **только Pro-пользователям**;
  - защищён idempotency через `X-Request-Id` и `request_dedup`.
- Исправлена сериализация ответа idempotency:
  - `pet_id` приводится к строке перед сохранением в `request_dedup.response_json`.
- Улучшено логирование:
  - добавлен явный лог успешного сохранения  
    `PETS_ACTIVE_SAVE ok=True user_id=… pet_id=…`.

#### Telegram-бот
- Кнопка **«💾 Сохранить изменения»** переведена на новый endpoint `/v1/pets/active/save`.
- `/v1/chat/ask` используется **только для вопросов**, без побочных сохранений.
- Реализован и стабилизирован **dirty-guard**:
  - предупреждение при выходе с несохранёнными изменениями;
  - варианты: сохранить / не сохранять / остаться;
  - защита от дабл-клика сохранения.
- Бот окончательно зафиксирован как **тонкий клиент**:
  - вопросы → `/v1/chat/ask`;
  - сохранение профиля → `/v1/pets/active/save`.

Проверка:
- Нажатие **«💾 Сохранить изменения»** → `POST /v1/pets/active/save` → `200 OK`.
- Быстрое двойное нажатие сохранения → профиль сохраняется **один раз**.
- Задание вопроса → **только** `POST /v1/chat/ask`, без сохранения профиля.
- В логах backend:
  - `PETS_ACTIVE_SAVE ok=True …`
  - отсутствие save-логики в `/chat/ask`.

## 2026-01-11  
### Backend + Telegram-бот — minimal profile contract (Free/Pro) + очистка `pet_profile`

#### Backend
- Зафиксирован **контракт “minimal profile” (Free / Pro)** для `POST /v1/chat/ask`:
  - **Pro + minimal `{type}`** → профиль подтягивается из **БД**;
  - **Free + minimal `{type}`** → профиль используется **только из request** (БД не трогается);
  - **Pro + rich payload** → профиль используется **из request**, без перезаписи данными из БД.
- В ответ `/v1/chat/ask` добавлено явное поле:
  - `meta.pet_profile_source: request | db | none`;
  - (опционально) `meta.pet_profile_pet_id` — для дебага и трассировки.
- Поведение backend закреплено как **явный контракт**, а не побочный эффект.

#### Smoke / тесты
- Добавлен smoke-скрипт:
  - `backend/scripts/smoke_min_profile_contract.ps1`.
- Скрипт проверяет 3 сценария:
  1) Pro + `{type}` → `meta.pet_profile_source=db`;
  2) Free + `{type}` → `meta.pet_profile_source=request`;
  3) Pro + rich profile → `meta.pet_profile_source=request`.
- Smoke задокументирован:
  - добавлен раздел **4.3 “Minimal profile contract (Free/Pro)”** в `docs/DEV_SMOKE.md`.
- Smoke успешно пройден локально:
  - контракт Free/Pro подтверждён;
  - защита от регрессий обеспечена.

#### Telegram-бот
- Реализована **локальная очистка `pet_profile` перед отправкой в `/v1/chat/ask`**:
  - из payload удаляются служебные поля бота  
    (`step`, `context`, `current_mode`, `question`);
  - очистка выполняется **рекурсивно** и применяется **только к копии**;
  - локальный state бота **не мутируется**.
- Очистка применяется **только для `/v1/chat/ask`**:
  - сохранение профиля через `/v1/pets/active/save` не затронуто.
- Добавлен отладочный лог при `BOT_DEBUG=1`:
  - `[PET_PROFILE_CLEAN] removed_keys=[...]`.
- Бот зафиксирован как **чистый клиент**:
  - в backend передаются только бизнес-данные профиля питомца;
  - снижен шум в логах и риск утечки служебных полей.

Проверка:
- Запуск `smoke_min_profile_contract.ps1` → `OK: minimal profile contract`.
- Free-сценарий: кнопки → описание питомца → вопрос → второй вопрос — работает стабильно.
- В логах backend:
  - `CHAT_PET_PROFILE source=db` / `source=request` в ожидаемых сценариях;
  - отсутствие `step/context/...` в `pet_profile`.

## 2026-01-11  
### Backend + Telegram-бот — Pro-память диалога + канонизация `health`

#### Telegram-бот
- Исправлен критичный баг Pro-режима:
  - Pro-пользователь теперь может **задавать несколько вопросов подряд** без ухода в анкету или вспомогательные меню.
- Уточнена логика Pro-гейта при обработке вопроса:
  - анкета запускается **только при реальном отсутствии `pet_profile.type`**;
  - при наличии `pet_profile` чат **всегда продолжается**.
- Поведение Free-пользователей не изменено.

Проверка:
- Pro: 2+ последовательных вопроса → ответы приходят стабильно.
- Free: поведение без изменений.

---

#### Backend — память диалога (sessions)
- Реализовано **разделение TTL сессий для Free и Pro**:
  - Free → `SESSION_TTL_MIN`;
  - Pro → `PRO_SESSION_TTL_MIN` (env).
- TTL выбирается **по плану пользователя** при каждом сохранении turn’а диалога.
- Улучшено логирование сессий:
  - `SESSION_DEBUG plan=... ttl_min=... turns=N` (без пользовательского текста).
- Реализация выполнена **без миграций БД**.

Проверка:
- В таблице `sessions`:
  - для Pro `expires_at` выставляется значительно дальше текущего времени;
  - для Free — в пределах `SESSION_TTL_MIN`.

---

#### Backend — нормализация блока `health`
- Зафиксирован **канонический формат хранения `health`** в `pets.profile`:
```json
{
  "health": {
    "notes_by_tag": {
      "allergy": "...",
      "gi": "...",
      "skin_coat": "...",
      "mobility": "...",
      "other": "..."
    }
  }
}
```
- При сохранении профиля (`POST /v1/pets/active/save`):
  - `health.tags` **удаляется** (не хранится в БД);
  - сохраняется **только `health.notes_by_tag`**;
  - неизвестные / legacy-ключи аккуратно добавляются в `other`;
  - пустой `health` удаляется целиком.
- Нормализация выполняется:
  - после `normalize_pet_dict()`;
  - **до merge/upsert**, чтобы в БД попадал только канон.
- Блоки `vaccines`, `parasites`, `owner_note` не изменялись (уже каноничны для MVP).

Проверка:
- После `/v1/pets/active/save` в БД отсутствует `health.tags`.
- В `pets.profile.health` хранится только `notes_by_tag` в каноне.

---

#### Итог
- Pro-чат стал устойчивым и многошаговым.
- Память диалога Free и Pro чётко разделена по TTL.
- Блок `health` зафиксирован как backend-контракт и больше не зависит от UI.
- Изменения минимальные, безопасные, локально проверены и закоммичены.

## 2026-01-12  
### Telegram-бот — экран ⭐ «Мой питомец» (summary) + централизация UI

#### Telegram-бот — ⭐ «Мой питомец»
- Реализован **summary профиля питомца** на экране ⭐ «Мой питомец»:
  - по умолчанию показывается **краткий вид** (3–5 строк);
  - добавлена кнопка **«📋 Показать профиль»** → разворачивает полный профиль;
  - добавлена кнопка **«🙈 Скрыть профиль»** → возвращает краткий вид.
- Переключение short/full выполнено через **`edit_message_text`**:
  - без спама в чат;
  - с fallback на отправку нового сообщения при ошибке edit.
- Форматирование профиля:
  - `dog / cat` — вид + кличка + базовые поля;
  - `other` — используется `animal_kind` (без слова «Другое», если вид задан);
  - блоки `health` и `owner_note` показываются **только в full** и с лимитами длины.
- Поведение Free / Pro не изменено, backend не затронут.

#### Telegram-бот — централизация UI
- Введены единые модули UI:
  - `ui/texts.py` — **все пользовательские тексты** (меню, help, ошибки, Pro-required);
  - `ui/keyboards.py` — **все UI-клавиатуры** (главное меню, выбор питомца, режимы, ⭐ «Мой питомец» short/full).
- `ui/main_menu.py` упрощён до тонкого рендера:
  - `TEXT_MAIN_MENU` + `kb_main_menu()`.
- Обновлены handlers:
  - `handlers/menu.py`, `handlers/question.py`, `flows/pro_flow.py` переведены на `TEXT_*` и `kb_*`;
  - **callback_data сохранены без изменений** (роутинг не сломан).
- Снижён риск UX-регрессий:
  - тексты и клавиатуры правятся централизованно;
  - уменьшена “рассыпуха” по handlers.

#### Проверка
- `/start` → главное меню отображается корректно.
- ℹ️ «Как это работает» → корректный текст + кнопка Home.
- ⭐ «Мой питомец»:
  - short summary по умолчанию;
  - show/hide работают;
  - ❓ Задать вопрос / ✏️ Изменить профиль — без регрессий.
- Free / Pro сценарии не изменены.

## 2026-01-12  
### Telegram-бот — фиксы Pro UX (возврат в ⭐ «Мой питомец» + skip имени)

- Починен переход из Pro post-меню:
  - «Вернуться к вопросу» теперь возвращает на экран ⭐ «Мой питомец» (short), а не в ввод вопроса;
  - dirty-guard: «Не сохранять» тоже возвращает на ⭐ «Мой питомец» (short).
- В `pro_flow.py` добавлен локальный рендер/лоадер short-экрана:
  - минимальный summary (type/name/age) + кеширование профиля;
  - fallback на `reply()`, если `edit_text()` не проходит;
  - нормализация `active pet` ограничена short-полями.
- В анкету Pro добавлена кнопка «Пропустить» на шаге имени:
  - вопрос “Как зовут питомца?” теперь показывает inline-кнопку skip;
  - обработчик `pro_name:skip` переводит сразу на шаг возраста.

## 2026-01-12  
### Pro Vision — загрузка и анализ фото (Variant A, inline base64)

#### Telegram-бот
- Добавлена поддержка **загрузки фото для Pro-пользователей**:
  - фото скачивается из Telegram, сжимается (Pillow) и кодируется в base64;
  - изображение всегда отправляется в backend (без локального определения плана).
- Реализован корректный UX для Free/Pro:
  - **Free** → backend возвращает `402 pro_required`, бот показывает upsell;
  - **Pro** → фото + текст обрабатываются через vision-модель.
- Исправлена совместимость с Pyrogram:
  - `message.photo` корректно обрабатывается как объект или список размеров;
  - добавлен guard на отсутствие `file_id`.

#### Backend
- В `/v1/chat/ask` добавана поддержка **attachments (image)**:
  - при наличии изображения принудительно выбирается `policy=pro_vision`;
  - provider = `openrouter`, модель берётся из `OPENROUTER_VISION_MODEL`;
  - добавлен guard `503 openrouter_not_configured`, если ключ не задан.
- Реализована стабильная маршрутизация OpenRouter:
  - `OPENROUTER_BASE_URL` используется как корень API;
  - конечный URL `/chat/completions` собирается клиентом безопасно.
- В ответ API добавлены диагностические поля:
  - `meta.llm_provider`
  - `meta.llm_model`
  - `meta.policy_name`

#### LLM / Prompts
- Реализована multimodal-сборка сообщений:
  - `content = [{type: "text"}, {type: "image_url"}]`;
  - формат `data:image/jpeg;base64,...`.
- В `prompts.py` добавлен `VISION_PREFIX`:
  - явно разрешает анализ изображений;
  - запрещает ответы вида «я не вижу изображения»;
  - применён ко всем режимам (`care`, `vaccines`, `emergency`).
- Добавлены диагностические логи:
  - `LLM_MESSAGES_IMAGE` — наличие image-part и длина data URL;
  - `LLM_HTTP` — provider, model, итоговый URL, multimodal-флаг.

#### Примечания
- `qwen/qwen-vl-plus` через OpenRouter не работает с Variant A (data URL);
- рабочая модель по умолчанию: `openai/gpt-4o-mini` (через OpenRouter);
- архитектура подготовлена для перехода на Variant B (`/media/init`, https URL).

## 2026-01-13
### Pro Vision — квоты, отказоустойчивость и UX ошибок

#### Backend
- Добавлена защита от списания квоты при некорректной обработке изображений:
  - введён **vision refusal guard** — если модель возвращает шаблонный отказ
    (например, «я не вижу изображение»), запрос считается неуспешным;
  - в этом случае backend возвращает `502 vision_not_processed`,
    а **месячная квота Pro Vision не уменьшается**.
- Улучшена диагностируемость:
  - при срабатывании guard логируется событие
    `VISION_GUARD refused=True` с `request_id`
    и кратким excerpt ответа модели (до 250 символов).
- Поведение подтверждено в реальных сценариях
  (чёрное/неинформативное изображение → отказ, корректное фото → 200 OK).

#### Telegram-бот
- Добавлен отдельный UX для ошибки `502 vision_not_processed`:
  - пользователь получает понятное сообщение с рекомендацией
    отправить другое, более качественное фото;
  - общий текст «Сервис временно недоступен» больше не используется
    для vision-отказов.
- UX лимитов Pro Vision доведён до консистентного состояния:
  - `402 vision_limit_exceeded` отображается отдельным сообщением;
  - ошибки vision и ошибки доступности backend чётко разделены.

Итог:
- Pro Vision стал устойчивым к сбоям vision-моделей;
- квоты списываются **только за реально обработанные изображения**;
- пользователь не теряет попытки и понимает, что делать дальше.

## 2026-01-13  
### Pro Vision — UX help и подсказка по качеству фото

#### Telegram-бот
- Обновлён экран **ℹ️ «Как это работает»**:
  - добавлено краткое описание возможности анализа фото в Pro;
  - без деталей лимитов и технических условий (компактный UX).
- Добавлена подсказка по качеству фото:
  - если запрос содержит изображение (`attachments`),
    в конец ответа добавляется рекомендация по съёмке;
  - для текстовых запросов подсказка не показывается.
- Логика фото не усложнялась:
  - без новых кнопок;
  - без state / session-флагов;
  - фото остаётся частью вопроса (caption + image).

Проверка:
- Открыть ℹ️ «Как это работает» → отображается строка про 📷 Pro.
- Отправить фото → ответ + подсказка по качеству.
- Отправить текст без фото → подсказки нет.

## 2026-01-13
### Backend — декомпозиция routes_chat.py (без изменения поведения)

#### Backend
- Выполнен безопасный рефактор `routes_chat.py`:
  - файл разделён на сервисы без изменения API, JSON-ответов и статус-кодов;
  - логика роутов сохранена, файл стал тонким orchestrator’ом.
- Вынесены и переиспользуются сервисы:
  - `request_dedup.py` — idempotency (`X-Request-Id`) и dedup-логика;
  - `pet_profile_service.py` — нормализация и определение effective pet profile;
  - `limits_service.py` — daily limit + cooldown (Free/Pro).
- Поведение полностью сохранено:
  - контракты `/v1/chat/ask` и `/v1/pets/active/save` **не изменены**;
  - тексты ошибок, upsell, лимиты и поля ответа — без изменений;
  - ключи логов (`CHAT_*`, `VISION_*`, `PETS_ACTIVE_SAVE`) сохранены.

#### Smoke
- Проверены сценарии:
  - missing / invalid `X-Request-Id`;
  - dedup (`X-Dedup-Hit=1`);
  - Free + image → `402 pro_required`;
  - daily limit + cooldown → `429`;
  - `/pets/active/save` Free / Pro.
- Регрессий не выявлено.

## 2026-01-13
### Backend — dev-only dotenv и защита от больших payload

- `.env` загружается только в dev (`ENV=dev`), `override=False` — прод окружение не перетирается значениями из файла.
- Для `/v1/chat/ask` добавлены ограничения входных данных:
  - `text` ограничен до 8000 символов;
  - `attachments` валидируются (raw base64 или `data:*;base64,`), декодируются и режутся по лимиту 3 MB (decoded bytes).
- Добавлены понятные ошибки валидации:
  - `invalid_attachment_base64`
  - `attachment_too_large`
- В документации dev-токен заменён на плейсхолдер `YOUR_DEV_TOKEN`.

## 2026-01-14
### Telegram-бот — hardening медиа/логов и корректный старт Pro после рестарта

- Debug-логи входящих сообщений: только `text_len` + `preview` + флаги медиа (без полного текста).
- Фото: отказ при неизвестном размере, защита от PIL decompression bomb/битых изображений.
- Voice/audio/document/video/sticker: понятный ответ “поддерживаю только текст и фото”.
- Pro больше не попадает в Free-онбординг после рестарта бота; при backend down — нейтральное сообщение вместо Free-текста.
- Добавлен `docs/FILEMAP.md` — карта проекта с ключевыми файлами, потоками и быстрыми ссылками “где менять X”.

## 2026-01-15
### Backend — prompt-eval стенд для контроля качества ответов LLM

- Добавлен **prompt-eval стенд** для `/v1/chat/ask`:
  - runner `backend/scripts/prompt_eval_run.py` для прогона кейсов;
  - набор кейсов `backend/scripts/prompt_eval_cases.jsonl`;
  - изолированное сохранение результатов в `backend/scripts/_eval_runs/<run_id>/`
    (`report.txt`, `summary.json`, `results.json`).
- Введены эвристики качества ответов (не медицинская валидация):
  - `structure` — наличие списков / секций;
  - `has_paragraphs` — ответ не “простыня”, разбит на абзацы;
  - `followup_q` — уточняющий вопрос;
  - `followup_time` — указание таймфрейма наблюдения;
  - `when_to_vet` — обозначение границы ответственности (по корням + действию).
- Эвристики доработаны для снижения ложных фейлов:
  - мягкие формулировки “к ветеринару” корректно детектятся;
  - расширен словарь таймфреймов (часы/дни/недели);
  - error / non-200 ответы не загрязняют статистику missing.
- Добавлен флаг `has_paragraphs` и включён в ожидания pet-кейсов —
  позволил отделить “читабельные ответы” от реальных проблем структуры.
- `_eval_runs/` добавлен в `.gitignore`, eval не влияет на production-код.

Назначение:
- зафиксировать измеримую базу качества ответов
  перед правкой промптов, моделей и UX;
- использовать eval как dev-инструмент, а не как прод-гейт.
